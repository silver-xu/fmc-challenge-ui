import { CSSProperties } from "react";

export const cardStyles = {
  margin: "30px",
} as CSSProperties;

export const numericStyles = {
  textAlign: "right",
} as CSSProperties;

export const centreAlign = {
  textAlign: "center",
} as CSSProperties;
